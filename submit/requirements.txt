# 평가 서버 제출용 requirements.txt
# script.py (HGBM + CNN 앙상블)에 필요한 패키지

# 평가 서버에 기본 설치된 패키지 (requirements.txt에 포함하지 않음):
# - numpy==1.26.4
# - pandas==2.2.2
# - scikit-learn==1.5.2 (HistGradientBoostingClassifier, CalibratedClassifierCV 등 사용)
# - torch==2.8.0+cpu (CNN 모델 사용)
# - torchaudio==2.8.0+cpu
# - torchvision==0.23.0+cpu

# script.py에서 사용하는 모든 패키지가 서버에 기본 설치되어 있으므로
# 추가 설치가 필요한 패키지는 없습니다.
# (pickle, json, collections, typing 등은 Python 표준 라이브러리)

